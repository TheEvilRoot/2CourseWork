sudo: require
language: cpp
os: osx
compiler: gcc

addons:
 homebrew:
  update: true
  packages:
  - qt

before_install:
 - export PATH="/usr/local/opt/qt/bin:$PATH"
before_script:
 - mkdir build
 - cd build
script:
 - qmake ../CourseWork_EnglishTests.pro
 - make
