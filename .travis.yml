sudo: require
language: cpp
os: osx
compiler: gcc

addons:
 homebrew:
  update: true
  packages:
  - qt

before_install:
 - export PATH="/usr/local/opt/qt/bin:$PATH"
before_script:
 - mkdir Build
 - cd Build
script:
 - qmake ../Source
 - make
